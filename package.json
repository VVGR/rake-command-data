{
  "name": "rake-command-data",
  "version": "1.1.9",
  "description": "download mock data form rap system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "download-file": "^0.1.5"
  }
}
